<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.lovebike">

    <!-- 권한 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:supportsRtl="true"
        android:theme="@style/Theme.Lovebike">

        <!-- 구글 맵 API 키 -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBZmbsUpURvPkh6TWeiQCHyAhkYhluQNfk" />

        <!-- 화면 액티비티 -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TrackingActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".OwnerFaceActivity" />
        <activity android:name=".WatchVideoActivity" />
        <activity android:name=".ParkingMapActivity" />
        <activity android:name=".ui.theme.OwnerFaceActivity" android:exported="false" />

        <!-- FCM 서비스 -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
